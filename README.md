# QA-automation-test
Automated login test using Selenium WebDriver with JavaScript. The script opens a browser, fills in login credentials, submits the form, verifies the success message using CSS selectors, and captures screenshots for test results. Demonstrates UI automation, XPath/CSS element selection, and basic test validation.
